# react-native-office-dogs-example

## How To Run
In the terminal, Navigate to the root folder, make sure you see package.json file in the folder and run
```bash
npm install
npm start
```
Than run:
```bash
cd ios
pod install
```
Open OfficeDogs.xcworkspace in Xcode 7 and hit the start button.
And that's it!
